# 0.1.0

* Fixed crash when quitting a rotated file
* Updated emulator so that it tests the whole app and not only the inner part
* Implement reboot and restart app views
* Eventually re-invert display colours on exit
* Implement SSH server based upon an existent KOReader installation
* Implement battery health viewer and fix battery management
* The clock is now synchronised with the system
