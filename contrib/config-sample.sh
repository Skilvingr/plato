# Don't set the framebuffer's depth.
# unset KAESAR_SET_FRAMEBUFFER_DEPTH

# Don't convert StarDict dictionaries.
# unset KAESAR_CONVERT_DICTIONARIES

# Disable hyphenation.
# [ -d hyphenation-patterns ] && rm -rf hyphenation-patterns
